using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestScript : CustomMonoBehaviorWrapper
{
    // Start is called before the first frame update
    void Start()
    {
        Log("Hello World");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
